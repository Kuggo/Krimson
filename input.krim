
// variables
var: int = 1

var = 2

// functions
inc: fn = (i: nat) -> out: nat {
    out = i + 1
}

plus1: nat -> nat = inc

// types
Coord: type = (nat, nat)

XY: type = {
    x: nat
    y: nat
}

AllowedOptions: type = {
  | NoSizeOption
  | AliasOption = int
  | TupleOption = (int, int)
  | ProductOption = {x: int y: int}
  | EnumInception = {
      | A
      | B
    }
}
